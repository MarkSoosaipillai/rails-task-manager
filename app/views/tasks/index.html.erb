<br>

<ul>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %> <%= link_to '🗑', destroy_task_path(task), method: :delete, data: { confirm: 'You sure about this?' } %></li>
<% end %>
</ul>

<%= link_to 'Add New Task', new_task_path %>
